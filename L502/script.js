var users = document.getElementById('users');
var empty = document.getElementById('empty-list');

alert("users ცარიელია: " + (users.children.length == 0));
alert("empty-list ცარიელია: " + (empty.children.length == 0));